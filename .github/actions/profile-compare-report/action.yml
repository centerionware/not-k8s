name: Render comparison
description: Shared profiling steps for source and published binaries.
runs:
  using: composite
  steps:
    - name: Fetch and render
      shell: bash
      run: |
        set -euo pipefail
        gh auth setup-git
        git clone --filter=blob:none --no-checkout --depth=1 --single-branch --branch "$PROFILE_RESULTS_BRANCH" "https://github.com/$GITHUB_REPOSITORY.git" profile-results
        git -C profile-results sparse-checkout set --no-cone "/comparisons/$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT/"
        git -C profile-results checkout "$PROFILE_RESULTS_BRANCH"
        python3 -m pip install --quiet --break-system-packages matplotlib numpy
        python3 deploy/render-profile-comparison.py --input "profile-results/comparisons/$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT" --output comparison-report
    - name: Publish report
      shell: bash
      run: bash deploy/publish-profile-comparison.sh report "$GITHUB_WORKSPACE/comparison-report"
