
fn reconcile_runtime_managed_fields(
    schema: &Value,
    existing: &Value,
    mut object: Value,
    manager: &str,
    operation: &str,
    subresource: &str,
    group: &str,
    version: &str,
) -> Value {
    include!("body-75-1.rs");
    include!("body-75-2.rs");
}
